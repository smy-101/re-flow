## ADDED Requirements

无新增需求。后端 API 能力已存在于 `openspec/specs/feed-management/spec.md`：
- Update feed settings（更新订阅设置）
- Delete feed subscription（删除订阅）

本次变更仅涉及前端 UX 实现，不修改后端行为或 API 契约。

## MODIFIED Requirements

无需求变更。后端 API 行为保持不变。

## REMOVED Requirements

无。
