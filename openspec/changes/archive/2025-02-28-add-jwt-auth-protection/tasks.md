## 1. 基础设施

- [x] 1.1 安装 `jose` 依赖（`pnpm add jose`）

## 2. 核心实现

- [x] 2.1 创建 `middleware.ts` 并实现 JWT 验证逻辑
- [x] 2.2 配置公开路由白名单（`/login`, `/register`, `/api/auth/*`）
- [x] 2.3 从 Cookie 中读取 `token` 并验证
- [x] 2.4 实现验证失败时重定向到 `/login`
- [x] 2.5 配置 `JWT_SECRET` 环境变量（确保与登录接口一致）

## 3. 测试验证

- [ ] 3.1 验证未登录访问受保护页面时重定向到 `/login`（需手动测试）
- [ ] 3.2 验证有效 token 可访问受保护页面（需手动测试）
- [ ] 3.3 验证登录/注册页面无需认证即可访问（需手动测试）
- [x] 3.4 运行 `pnpm test` 并确认全部通过（无失败/无报错）

## 4. 类型检查

- [x] 4.1 运行 `pnpm exec tsc --noEmit` 确认零类型错误
