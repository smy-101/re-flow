# No Specification Changes

This change is a pure renaming operation (middleware → proxy) with no behavioral changes.

**Summary:**
- File rename: `middleware.ts` → `proxy.ts`
- Function rename: `export function middleware()` → `export function proxy()`
- No changes to authentication logic or routing behavior

Since this is an API naming change only, there are no new or modified requirements to document.
